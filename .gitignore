# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr

# The plugin build. `build/` because that is one of the four places the
# community directory's scanner looks for main.js — see CLAUDE.md.
build
*.local

# .env.local names the Obsidian vault to deploy the plugin into — one machine's
# path, not the project's. See README.md.

# Agent scratch space — holds git worktrees with their own node_modules
.claude

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
